
            const addresses = require("./addresses.json")
            const abi = require("./abi.json")
            
            module.exports = {
                abi,
                addresses,
            }
        